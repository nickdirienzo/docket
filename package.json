{
	"name": "docket",
	"private": true,
	"workspaces": ["worker", "mcp-server", "slack-app"],
	"scripts": {
		"check": "npm run lint && npm run typecheck && npm run test",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"typecheck": "tsc --build",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}
